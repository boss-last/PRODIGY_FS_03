services:
  mysql:
    image: mysql:8.0
    container_name: mama_africa_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: mama_africa_db
    ports:
      - "3388:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
